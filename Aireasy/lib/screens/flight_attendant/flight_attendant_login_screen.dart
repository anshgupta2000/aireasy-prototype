import 'package:flutter/material.dart';

class FlightAttendantLoginScreen extends StatelessWidget {
  const FlightAttendantLoginScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Flight Attendant Login Screen'),
      ),
      body: const Center(
        child: Text('Flight Attendant Login Screen'),
      ),
    );
  }
}
